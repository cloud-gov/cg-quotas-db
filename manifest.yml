---
# all applications use these settings and services
memory: 512m
applications:
- name: quotas
  buildpack: python_buildpack
  stack: cflinuxfs2
  command: ./cf-startup.sh
  services:
  - rds-cg-quotas-db
